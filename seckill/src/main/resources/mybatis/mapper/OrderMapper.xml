<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yaqi.seckill.dao.OrderMapper">
    <insert id="insertOrderToDb" parameterType="Order">
            insert into orders(userName, goodsId, goodsName, createDate) values (#{userName}, #{goodsId}, #{goodsName}, #{createDate})
    </insert>
</mapper>